apiVersion: apps/v1
kind: Deployment
metadata:
  name: poi
spec:
  replicas: 2
  selector:
    matchLabels:
      app: poi    
  template:
    metadata:
      labels:
        app: poi
    spec:
      containers:
      - image: registryxaj9926.azurecr.io/tripinsights/poi:latest
        name: poi
        ports:
          - containerPort: 80
        envFrom:
          - configMapRef:
              name: trip-config
        resources: {}
        terminationMessagePath: /termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30   
         